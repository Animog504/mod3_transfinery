let translateURL = 'https://translation.googleapis.com'

translate:	POST /language/translate/v2 
Translates input text, returning translated text.

detect:	POST /language/translate/v2/detect 
Detects the language of text within a request.

languages:	GET /language/translate/v2/languages 
Returns a list of supported languages for translation.
